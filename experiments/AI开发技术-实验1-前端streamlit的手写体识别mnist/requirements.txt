# ============================================================
# 手写数字识别 MNIST + Streamlit 实验 —— 所需 Python 库
# 安装方式: pip install -r requirements.txt
# 项目路径:
#   Agent+Skills/     — PyTorch 版 (主项目)
#   使用prompt/       — TensorFlow 版 (备选)
# ============================================================

# ---------- 深度学习框架 (二选一) ----------
# PyTorch 版 (主项目 Agent+Skills/ 使用)
torch>=2.0.0
torchvision>=0.15.0

# TensorFlow 版 (备选 使用prompt/ 使用)
# tensorflow>=2.13.0

# ---------- 数值计算与图像处理 ----------
numpy>=1.24.0
pillow>=10.0.0

# opencv-python 仅在 TensorFlow 版 (使用prompt/app.py) 中使用
# opencv-python>=4.8.0

# ---------- Streamlit 前端 ----------
streamlit>=1.28.0
streamlit-drawable-canvas>=0.9.0

# ---------- 可视化 ----------
matplotlib>=3.7.0

# ---------- 进度条 ----------
tqdm>=4.65.0

# ---------- 测试 ----------
pytest>=7.4.0
